language: go
sudo: false
go:
  - tip
os:
  - linux
install:
  - go get -u -t -v ./...
script:
  - go test ./...
after_success:
  - bash <(curl --request POST "https://goreportcard.com/checks" --data "repo=github.com/ReanGD/go-web-search")
notifications:
  email:
    recipients:
      - reangd@yandex.ru
    on_success: never
    on_failure: always
